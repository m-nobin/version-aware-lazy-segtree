[project]
name = "valseg-bench-analysis"
version = "0.1.0"
description = "Statistics, figures and LaTeX tables for the persistence benchmark campaign"
requires-python = ">=3.11"
dependencies = [
  "matplotlib>=3.9",
  "numpy>=2.0",
  "pandas>=2.2",
  "scipy>=1.13",
  "seaborn>=0.13",
]
